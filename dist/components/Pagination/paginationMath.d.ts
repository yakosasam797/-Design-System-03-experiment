/** Mathematically valid pagination range helpers (1-based pages). */
export declare function pageCountFor(total: number, pageSize: number): number;
export declare function rangeLabel(page: number, pageSize: number, total: number): string;
